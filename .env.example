# 開発時はこのファイルを .env にコピーして使用
# cp .env.example .env

# 開発モード（true にするとLIFF/GASをモックで動作）
VITE_DEV_MODE=true

# 以下は校舎ごとに別の値を設定する（西明石 / 大久保 で別 LIFF・別 GAS）

# LINE Developers コンソールで取得した LIFF ID
VITE_LIFF_ID=

# GAS (Google Apps Script) デプロイURL
VITE_GAS_URL=
